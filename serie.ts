export class Serie{

    constructor(public numero: number, public nombre: string,public plataforma: string, public temporadas: number, public info: string, public link: string, public imagen: string)
    {

    }
}